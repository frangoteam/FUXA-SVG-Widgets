<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 120 300" preserveAspectRatio="xMidYMid meet">
  <defs>
    <filter id="lgShadow" x="-30%" y="-30%" width="160%" height="160%">
      <feDropShadow dx="0" dy="2" stdDeviation="2" flood-color="#000000" flood-opacity="0.22"/>
    </filter>
  </defs>

  <rect data-lg="background" x="0" y="0" width="120" height="300" fill="#ffffff" opacity="0"/>

  <g data-lg="scaleLayer">
    <line x1="44" y1="244" x2="56" y2="244" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="244" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">0</text>
    <line x1="50" y1="234.18" x2="56" y2="234.18" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="224.36" x2="56" y2="224.36" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="224.36" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">20</text>
    <line x1="50" y1="214.55" x2="56" y2="214.55" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="204.73" x2="56" y2="204.73" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="204.73" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">40</text>
    <line x1="50" y1="194.91" x2="56" y2="194.91" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="185.09" x2="56" y2="185.09" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="185.09" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">60</text>
    <line x1="50" y1="175.27" x2="56" y2="175.27" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="165.45" x2="56" y2="165.45" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="165.45" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">80</text>
    <line x1="50" y1="155.64" x2="56" y2="155.64" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="145.82" x2="56" y2="145.82" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="145.82" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">100</text>
    <line x1="50" y1="136" x2="56" y2="136" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="126.18" x2="56" y2="126.18" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="126.18" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">120</text>
    <line x1="50" y1="116.36" x2="56" y2="116.36" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="106.55" x2="56" y2="106.55" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="106.55" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">140</text>
    <line x1="50" y1="96.73" x2="56" y2="96.73" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="86.91" x2="56" y2="86.91" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="86.91" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">160</text>
    <line x1="50" y1="77.09" x2="56" y2="77.09" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="67.27" x2="56" y2="67.27" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="67.27" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">180</text>
    <line x1="50" y1="57.45" x2="56" y2="57.45" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="47.64" x2="56" y2="47.64" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="47.64" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">200</text>
    <line x1="50" y1="37.82" x2="56" y2="37.82" stroke="#4b4b4b" stroke-width="1"/>
    <line x1="44" y1="28" x2="56" y2="28" stroke="#4b4b4b" stroke-width="2"/>
    <text x="38" y="28" text-anchor="end" dominant-baseline="middle" fill="#444444" font-family="Arial, Helvetica, sans-serif" font-size="10" font-weight="600">220</text>
  </g>

  <g data-lg="redZoneLayer">
    <rect x="45" y="28" width="10" height="117.82" fill="#d76363"/>
  </g>

  <rect data-lg="barFrame" x="62" y="28" width="18" height="216" fill="#d0d0d0" stroke="#404040" stroke-width="4"/>
  <rect data-lg="barEmpty" x="64" y="30" width="14" height="212" fill="#cfcfcf"/>
  <rect data-lg="barFill" x="64" y="242" width="14" height="0" fill="#8f8f8f"/>
  <line data-lg="valueLine" x1="44" y1="244" x2="80" y2="244" stroke="#555555" stroke-width="1.5" stroke-linecap="square"/>

  <text data-lg="unitText" x="60" y="268" text-anchor="middle" dominant-baseline="middle" fill="#8a8a8a" font-family="Arial, Helvetica, sans-serif" font-size="13" font-weight="600">°C</text>

  <script type="text/javascript"><![CDATA[
    //!export-start
      var _pn_value = 0;
      var _pn_min = 0;
      var _pn_max = 220;
      var _pn_redStart = 100;
      var _pn_redEnd = 220;
      var _pn_majorStep = 20;
      var _pn_minorDivisions = 2;
      var _pn_animationMs = 350;
      var _pn_scaleFontSize = 10;
      var _pn_redZoneWidth = 10;
      var _pn_barWidth = 18;
      var _pn_barStrokeWidth = 4;
      var _pn_valueLineWidth = 1.5;
      var _ps_scaleFontFamily = 'Arial, Helvetica, sans-serif';
      var _ps_scaleFontWeight = '600';
      var _ps_unit = '°C';
      var _pc_barBorderColor = '#404040';
      var _pc_barEmptyColor = '#cfcfcf';
      var _pc_barFillColor = '#8f8f8f';
      var _pc_tickColor = '#4b4b4b';
      var _pc_textColor = '#444444';
      var _pc_redZoneColor = '#d76363';
      var _pc_valueLineColor = '#555555';
      var _pc_unitColor = '#8a8a8a';
    //!export-end

    var TOP = 28;
    var BOTTOM = 244;
    var SCALE_X = 56;
    var LABEL_X = 38;
    var RED_X = 45;
    var BAR_X = 62;
    var currentValue = 0;
    var targetValue = 0;
    var timer = null;
    var svgRoot = null;

    function getSvgRoot() {
      if (svgRoot) { return svgRoot; }
      var script = null;
      if (typeof document !== 'undefined' && document.currentScript) {
        script = document.currentScript;
      }
      if (!script && typeof document !== 'undefined') {
        var scripts = document.getElementsByTagName('script');
        for (var i = 0; i < scripts.length; i++) {
          if (scripts[i].textContent && scripts[i].textContent.indexOf('linearGaugeVertical') >= 0) {
            script = scripts[i];
            break;
          }
        }
        if (!script && scripts.length > 0) { script = scripts[scripts.length - 1]; }
      }
      var node = script;
      while (node && node.tagName && node.tagName.toLowerCase() !== 'svg') {
        node = node.parentNode;
      }
      svgRoot = node;
      return svgRoot;
    }

    function getSvgElement(name) {
      var root = getSvgRoot();
      if (!root) { return null; }
      var items = root.getElementsByTagName('*');
      for (var i = 0; i < items.length; i++) {
        if (items[i].getAttribute('data-lg') === name) { return items[i]; }
      }
      return null;
    }

    function clamp(value, min, max) {
      return Math.max(min, Math.min(max, value));
    }

    function safeNumber(value, fallback) {
      var n = parseFloat(value);
      return isNaN(n) ? fallback : n;
    }

    function valueToY(value) {
      var min = safeNumber(_pn_min, 0);
      var max = safeNumber(_pn_max, 100);
      if (max === min) { max = min + 1; }
      var ratio = (clamp(value, min, max) - min) / (max - min);
      return BOTTOM - ratio * (BOTTOM - TOP);
    }

    function getMajorStep() {
      var min = safeNumber(_pn_min, 0);
      var max = safeNumber(_pn_max, 100);
      var configured = safeNumber(_pn_majorStep, 0);
      if (configured > 0) { return configured; }
      var range = Math.abs(max - min);
      if (range <= 10) { return 1; }
      if (range <= 50) { return 5; }
      if (range <= 120) { return 10; }
      if (range <= 300) { return 20; }
      return 50;
    }

    function createSvgElement(name) {
      return document.createElementNS('http://www.w3.org/2000/svg', name);
    }

    function clearChildren(node) {
      if (!node) { return; }
      while (node.firstChild) { node.removeChild(node.firstChild); }
    }

    function buildScale() {
      var scaleLayer = getSvgElement('scaleLayer');
      if (!scaleLayer) { return; }
      clearChildren(scaleLayer);

      var min = safeNumber(_pn_min, 0);
      var max = safeNumber(_pn_max, 100);
      var majorStep = getMajorStep();
      var minorDivisions = Math.max(0, parseInt(_pn_minorDivisions, 10) || 0);
      var start = Math.ceil(min / majorStep) * majorStep;

      for (var value = start; value <= max + majorStep * 0.001; value += majorStep) {
        if (value < min - majorStep * 0.001) { continue; }
        var y = valueToY(value);

        var major = createSvgElement('line');
        major.setAttribute('x1', String(SCALE_X - 12));
        major.setAttribute('y1', String(y));
        major.setAttribute('x2', String(SCALE_X));
        major.setAttribute('y2', String(y));
        major.setAttribute('stroke', _pc_tickColor);
        major.setAttribute('stroke-width', '2');
        scaleLayer.appendChild(major);

        var label = createSvgElement('text');
        label.setAttribute('x', String(LABEL_X));
        label.setAttribute('y', String(y));
        label.setAttribute('text-anchor', 'end');
        label.setAttribute('dominant-baseline', 'middle');
        label.setAttribute('fill', _pc_textColor);
        label.setAttribute('font-family', _ps_scaleFontFamily || 'Arial, Helvetica, sans-serif');
        label.setAttribute('font-size', String(safeNumber(_pn_scaleFontSize, 10)));
        label.setAttribute('font-weight', _ps_scaleFontWeight || '600');
        label.textContent = String(Math.round(value * 100) / 100);
        scaleLayer.appendChild(label);

        if (minorDivisions > 0 && value + majorStep <= max + majorStep * 0.001) {
          for (var m = 1; m < minorDivisions; m++) {
            var minorValue = value + (majorStep / minorDivisions) * m;
            if (minorValue >= min && minorValue <= max) {
              var my = valueToY(minorValue);
              var minor = createSvgElement('line');
              minor.setAttribute('x1', String(SCALE_X - 6));
              minor.setAttribute('y1', String(my));
              minor.setAttribute('x2', String(SCALE_X));
              minor.setAttribute('y2', String(my));
              minor.setAttribute('stroke', _pc_tickColor);
              minor.setAttribute('stroke-width', '1');
              scaleLayer.appendChild(minor);
            }
          }
        }
      }
    }

    function updateRedZone() {
      var layer = getSvgElement('redZoneLayer');
      if (!layer) { return; }
      clearChildren(layer);

      var min = safeNumber(_pn_min, 0);
      var max = safeNumber(_pn_max, 100);
      var redStart = clamp(safeNumber(_pn_redStart, max), min, max);
      var redEnd = clamp(safeNumber(_pn_redEnd, max), min, max);
      var y1 = valueToY(Math.max(redStart, redEnd));
      var y2 = valueToY(Math.min(redStart, redEnd));
      var width = Math.max(0, safeNumber(_pn_redZoneWidth, 10));

      var rect = createSvgElement('rect');
      rect.setAttribute('x', String(RED_X));
      rect.setAttribute('y', String(y1));
      rect.setAttribute('width', String(width));
      rect.setAttribute('height', String(y2 - y1));
      rect.setAttribute('fill', _pc_redZoneColor);
      layer.appendChild(rect);
    }

    function updateBarGeometry() {
      var frame = getSvgElement('barFrame');
      var empty = getSvgElement('barEmpty');
      var fill = getSvgElement('barFill');
      var valueLine = getSvgElement('valueLine');
      var w = Math.max(4, safeNumber(_pn_barWidth, 18));
      var strokeW = Math.max(0, safeNumber(_pn_barStrokeWidth, 4));
      var innerW = Math.max(1, w - strokeW);
      var x = BAR_X + (18 - w) / 2;
      var innerX = x + strokeW / 2;
      var innerTop = TOP + strokeW / 2;
      var innerHeight = Math.max(1, BOTTOM - TOP - strokeW);

      if (frame) {
        frame.setAttribute('x', String(x));
        frame.setAttribute('y', String(TOP));
        frame.setAttribute('width', String(w));
        frame.setAttribute('height', String(BOTTOM - TOP));
        frame.setAttribute('stroke-width', String(strokeW));
        frame.setAttribute('stroke', _pc_barBorderColor);
        frame.setAttribute('fill', _pc_barEmptyColor);
      }
      if (empty) {
        empty.setAttribute('x', String(innerX));
        empty.setAttribute('y', String(innerTop));
        empty.setAttribute('width', String(innerW));
        empty.setAttribute('height', String(innerHeight));
        empty.setAttribute('fill', _pc_barEmptyColor);
      }
      if (fill) {
        fill.setAttribute('x', String(innerX));
        fill.setAttribute('width', String(innerW));
        fill.setAttribute('fill', _pc_barFillColor);
      }
      if (valueLine) {
        valueLine.setAttribute('x1', String(SCALE_X - 12));
        valueLine.setAttribute('x2', String(x + w));
        valueLine.setAttribute('stroke', _pc_valueLineColor);
        valueLine.setAttribute('stroke-width', String(Math.max(0, safeNumber(_pn_valueLineWidth, 1.5))));
      }
    }

    function updateFill(value) {
      var fill = getSvgElement('barFill');
      var valueLine = getSvgElement('valueLine');
      var y = valueToY(value);
      if (fill) {
        fill.setAttribute('y', String(y));
        fill.setAttribute('height', String(BOTTOM - y));
      }
      if (valueLine) {
        valueLine.setAttribute('y1', String(y));
        valueLine.setAttribute('y2', String(y));
      }
    }

    function applyStyle() {
      var unit = getSvgElement('unitText');
      if (unit) {
        unit.textContent = _ps_unit || '';
        unit.setAttribute('fill', _pc_unitColor);
      }
      updateBarGeometry();
      buildScale();
      updateRedZone();
      updateFill(currentValue);
    }

    function animateTo(value) {
      targetValue = safeNumber(value, _pn_min);
      if (timer) {
        clearInterval(timer);
        timer = null;
      }

      var duration = Math.max(0, safeNumber(_pn_animationMs, 350));
      var start = currentValue;
      var delta = targetValue - start;
      if (duration === 0 || Math.abs(delta) < 0.0001) {
        currentValue = targetValue;
        updateFill(currentValue);
        return;
      }

      var startTime = new Date().getTime();
      timer = setInterval(function () {
        var elapsed = new Date().getTime() - startTime;
        var t = clamp(elapsed / duration, 0, 1);
        var eased = 1 - Math.pow(1 - t, 3);
        currentValue = start + delta * eased;
        updateFill(currentValue);
        if (t >= 1) {
          clearInterval(timer);
          timer = null;
          currentValue = targetValue;
          updateFill(currentValue);
        }
      }, 16);
    }

    function putValue(id, value) {
      switch (id) {
        case '_pn_value': _pn_value = safeNumber(value, _pn_value); animateTo(_pn_value); break;
        case '_pn_min': _pn_min = safeNumber(value, _pn_min); applyStyle(); animateTo(_pn_value); break;
        case '_pn_max': _pn_max = safeNumber(value, _pn_max); applyStyle(); animateTo(_pn_value); break;
        case '_pn_redStart': _pn_redStart = safeNumber(value, _pn_redStart); updateRedZone(); break;
        case '_pn_redEnd': _pn_redEnd = safeNumber(value, _pn_redEnd); updateRedZone(); break;
        case '_pn_majorStep': _pn_majorStep = safeNumber(value, _pn_majorStep); buildScale(); break;
        case '_pn_minorDivisions': _pn_minorDivisions = parseInt(value, 10); buildScale(); break;
        case '_pn_animationMs': _pn_animationMs = safeNumber(value, _pn_animationMs); break;
        case '_pn_scaleFontSize': _pn_scaleFontSize = safeNumber(value, _pn_scaleFontSize); buildScale(); break;
        case '_pn_redZoneWidth': _pn_redZoneWidth = safeNumber(value, _pn_redZoneWidth); updateRedZone(); break;
        case '_pn_barWidth': _pn_barWidth = safeNumber(value, _pn_barWidth); updateBarGeometry(); updateFill(currentValue); break;
        case '_pn_barStrokeWidth': _pn_barStrokeWidth = safeNumber(value, _pn_barStrokeWidth); updateBarGeometry(); updateFill(currentValue); break;
        case '_pn_valueLineWidth': _pn_valueLineWidth = safeNumber(value, _pn_valueLineWidth); updateBarGeometry(); break;
        case '_ps_scaleFontFamily': _ps_scaleFontFamily = value; buildScale(); break;
        case '_ps_scaleFontWeight': _ps_scaleFontWeight = value; buildScale(); break;
        case '_ps_unit': _ps_unit = value; applyStyle(); break;
        case '_pc_barBorderColor': _pc_barBorderColor = value; updateBarGeometry(); break;
        case '_pc_barEmptyColor': _pc_barEmptyColor = value; updateBarGeometry(); break;
        case '_pc_barFillColor': _pc_barFillColor = value; updateBarGeometry(); updateFill(currentValue); break;
        case '_pc_tickColor': _pc_tickColor = value; buildScale(); break;
        case '_pc_textColor': _pc_textColor = value; buildScale(); break;
        case '_pc_redZoneColor': _pc_redZoneColor = value; updateRedZone(); break;
        case '_pc_valueLineColor': _pc_valueLineColor = value; updateBarGeometry(); break;
        case '_pc_unitColor': _pc_unitColor = value; applyStyle(); break;
      }
    }

    function init() {
      currentValue = safeNumber(_pn_value, _pn_min);
      targetValue = currentValue;
      applyStyle();
      updateFill(currentValue);
    }

    if (typeof window !== 'undefined') {
      window.putValue = putValue;
    }

    init();
  ]]></script>
</svg>
