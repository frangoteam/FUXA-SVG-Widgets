<?xml version='1.0' encoding='UTF-8'?>
<svg xmlns="http://www.w3.org/2000/svg" xmlns:svg="http://www.w3.org/2000/svg" version="1.1" viewBox="0 0 42.709999 50" id="fuxaWidget" style="border: none !important; outline: none !important; box-shadow: none !important; margin: 0; padding: 0; background: transparent;" width="100%" height="100%">
  <g id="fuxaSymbol">
    <defs id="defs1"/>
    <g data-cell-id="gQ551Ar7PwXLAaA90adm-604" id="g6" transform="translate(-10.5,-0.5)">
      <g id="cell-gQ551Ar7PwXLAaA90adm-604">
        <g transform="translate(0.5,0.5)" id="g5">
          <path d="M 13.71,0 C 11.66,0 10,1.66 10,3.71 v 15.43 c 0,2.05 1.66,3.71 3.71,3.71 h 12.28 c 2.04,0 3.7,-1.66 3.7,-3.71 V 3.71 C 29.69,1.66 28.03,0 25.99,0 Z m 23.02,0 c -2.05,0 -3.71,1.66 -3.71,3.71 v 15.43 c 0,2.05 1.66,3.71 3.71,3.71 h 12.28 c 2.04,0 3.7,-1.66 3.7,-3.71 V 3.71 C 52.71,1.66 51.05,0 49.01,0 Z M 13.71,1.03 h 12.28 c 1.49,0 2.67,1.18 2.67,2.68 v 15.43 c 0,0.94 -0.46,1.75 -1.18,2.23 C 27.25,21.01 27.1,20.53 27.1,19.84 V 12.7 c 0,-4.97 -3.56,-8.29 -6.99,-8.29 -2.26,0 -4.14,1.06 -5.42,2.57 -1.28,1.52 -2,3.48 -2,5.37 v 7.36 c 0,0.74 -0.12,1.16 -0.26,1.41 -0.06,0.11 -0.12,0.19 -0.19,0.27 -0.73,-0.48 -1.21,-1.3 -1.21,-2.25 V 3.71 c 0,-1.5 1.18,-2.68 2.68,-2.68 z m 23.02,0 h 12.28 c 1.49,0 2.68,1.18 2.68,2.68 v 15.43 c 0,0.94 -0.47,1.75 -1.18,2.23 -0.23,-0.36 -0.38,-0.84 -0.38,-1.53 V 12.7 c 0,-4.97 -3.56,-8.29 -7,-8.29 -2.26,0 -4.13,1.06 -5.42,2.57 -1.28,1.52 -1.99,3.48 -1.99,5.37 v 7.36 c 0,0.74 -0.13,1.16 -0.27,1.41 -0.06,0.11 -0.12,0.19 -0.19,0.27 -0.73,-0.48 -1.21,-1.3 -1.21,-2.25 V 3.71 c 0,-1.5 1.19,-2.68 2.68,-2.68 z M 20.11,5.44 c 2.76,0 5.96,2.79 5.96,7.26 v 7.14 c 0,0.77 0.18,1.41 0.46,1.93 -0.18,0.03 -0.36,0.05 -0.54,0.05 H 13.71 c -0.17,0 -0.32,-0.02 -0.48,-0.04 0.04,-0.05 0.07,-0.11 0.1,-0.17 0.25,-0.44 0.39,-1.04 0.39,-1.9 v -7.36 c 0,-1.63 0.64,-3.38 1.76,-4.7 1.11,-1.32 2.69,-2.21 4.63,-2.21 z m 23.02,0 c 2.76,0 5.97,2.79 5.97,7.26 v 7.14 c 0,0.77 0.17,1.41 0.45,1.93 -0.17,0.03 -0.36,0.05 -0.54,0.05 H 36.73 c -0.16,0 -0.32,-0.02 -0.47,-0.04 0.03,-0.05 0.07,-0.11 0.1,-0.17 0.24,-0.44 0.39,-1.04 0.39,-1.9 v -7.36 c 0,-1.63 0.63,-3.38 1.75,-4.7 1.12,-1.32 2.69,-2.21 4.63,-2.21 z m -25.46,4.4 c -0.34,0 -0.67,0.14 -0.91,0.38 -0.24,0.24 -0.37,0.56 -0.37,0.91 0,0.34 0.13,0.66 0.37,0.9 0.24,0.24 0.57,0.38 0.91,0.38 0.71,0 1.28,-0.58 1.28,-1.28 0,-0.71 -0.57,-1.29 -1.28,-1.29 z m 4.4,0 c -0.35,0 -0.67,0.14 -0.91,0.38 -0.24,0.24 -0.38,0.56 -0.38,0.91 0,0.34 0.14,0.66 0.38,0.9 0.24,0.24 0.56,0.38 0.91,0.38 0.7,0 1.28,-0.58 1.28,-1.28 0,-0.71 -0.58,-1.29 -1.28,-1.29 z m 18.62,0 c -0.71,0 -1.28,0.58 -1.28,1.29 0,0.7 0.57,1.28 1.28,1.28 0.34,0 0.67,-0.14 0.91,-0.38 0.24,-0.24 0.38,-0.56 0.38,-0.9 0,-0.35 -0.14,-0.67 -0.38,-0.91 C 41.36,9.98 41.03,9.84 40.69,9.84 Z m 4.4,0 c -0.71,0 -1.28,0.58 -1.28,1.29 0,0.7 0.57,1.28 1.28,1.28 0.71,0 1.28,-0.58 1.28,-1.28 0,-0.71 -0.57,-1.29 -1.28,-1.29 z m -25.23,3.28 c -0.43,0 -0.88,0.1 -1.35,0.31 0,0 -0.01,0.01 -0.01,0.01 -1.34,0.66 -2.24,1.48 -3.06,2.18 l 0.06,0.02 c -0.13,0.09 -0.22,0.22 -0.24,0.37 0.02,0.15 0.11,0.29 0.24,0.37 l -0.05,0.02 c 0.79,0.67 1.81,1.37 2.94,1.91 0,0 0.01,0.01 0.01,0.01 0.97,0.42 2.16,0.44 3.07,-0.02 0,0 0,0 0,0 1.19,-0.61 2.04,-1.22 2.87,-1.81 0.12,-0.09 0.2,-0.23 0.21,-0.38 0.02,-0.16 -0.04,-0.31 -0.15,-0.41 -0.73,-0.69 -1.77,-1.64 -3.13,-2.27 0,0 -0.01,0 -0.01,0 -0.42,-0.19 -0.85,-0.3 -1.31,-0.31 v 0 c -0.03,0 -0.06,0 -0.09,0 z m 23.03,0 c -0.43,0 -0.88,0.1 -1.35,0.31 -0.01,0 -0.01,0.01 -0.02,0.01 -1.34,0.66 -2.24,1.48 -3.05,2.18 l 0.05,0.02 c -0.13,0.09 -0.21,0.22 -0.23,0.37 0.02,0.15 0.1,0.29 0.23,0.37 l -0.05,0.02 c 0.8,0.67 1.82,1.37 2.94,1.91 0.01,0 0.01,0.01 0.02,0.01 0.96,0.42 2.16,0.44 3.07,-0.02 0,0 0,0 0,0 1.18,-0.61 2.03,-1.22 2.86,-1.81 0.13,-0.09 0.21,-0.23 0.22,-0.38 0.01,-0.16 -0.05,-0.31 -0.16,-0.41 -0.73,-0.69 -1.76,-1.64 -3.13,-2.27 0,0 0,0 0,0 -0.42,-0.19 -0.86,-0.3 -1.32,-0.31 v 0 c -0.03,0 -0.05,0 -0.08,0 z m -23.01,1 c 0.02,0 0.03,0 0.05,0 v 0 c 0.29,0 0.57,0.09 0.91,0.24 0.95,0.44 1.7,1.06 2.34,1.64 -0.66,0.47 -1.29,0.93 -2.18,1.38 -0.53,0.27 -1.52,0.28 -2.18,0 C 18,16.98 17.3,16.46 16.64,15.96 17.3,15.4 18,14.84 18.95,14.37 c 0.36,-0.17 0.66,-0.25 0.93,-0.25 z m 23.02,0 c 0.02,0 0.04,0 0.06,0 v 0 c 0.29,0 0.56,0.09 0.9,0.24 0.95,0.44 1.7,1.06 2.35,1.64 -0.66,0.47 -1.3,0.93 -2.18,1.38 -0.53,0.27 -1.52,0.28 -2.18,0 -0.83,-0.4 -1.53,-0.92 -2.18,-1.42 0.66,-0.56 1.35,-1.12 2.3,-1.59 0.37,-0.17 0.66,-0.25 0.93,-0.25 z M 13.71,27.15 c -2.05,0 -3.71,1.66 -3.71,3.71 v 15.43 c 0,2.05 1.66,3.71 3.71,3.71 h 12.28 c 2.04,0 3.7,-1.66 3.7,-3.71 V 30.86 c 0,-2.05 -1.66,-3.71 -3.7,-3.71 z m 23.02,0 c -2.05,0 -3.71,1.66 -3.71,3.71 v 15.43 c 0,2.05 1.66,3.71 3.71,3.71 h 12.28 c 2.04,0 3.7,-1.66 3.7,-3.71 V 30.86 c 0,-2.05 -1.66,-3.71 -3.7,-3.71 z m -23.02,1.03 h 12.28 c 1.49,0 2.67,1.19 2.67,2.68 v 15.43 c 0,0.94 -0.46,1.75 -1.18,2.23 -0.23,-0.36 -0.38,-0.83 -0.38,-1.53 v -7.14 c 0,-4.96 -3.56,-8.28 -6.99,-8.28 -2.26,0 -4.14,1.06 -5.42,2.57 -1.28,1.51 -2,3.47 -2,5.36 v 7.36 c 0,0.74 -0.12,1.16 -0.26,1.41 -0.06,0.11 -0.12,0.2 -0.19,0.27 -0.73,-0.48 -1.21,-1.3 -1.21,-2.25 V 30.86 c 0,-1.49 1.18,-2.68 2.68,-2.68 z m 23.02,0 h 12.28 c 1.49,0 2.68,1.19 2.68,2.68 v 15.43 c 0,0.94 -0.47,1.75 -1.18,2.23 -0.23,-0.36 -0.38,-0.83 -0.38,-1.53 v -7.14 c 0,-4.96 -3.56,-8.28 -7,-8.28 -2.26,0 -4.13,1.06 -5.42,2.57 -1.28,1.51 -1.99,3.47 -1.99,5.36 v 7.36 c 0,0.74 -0.13,1.16 -0.27,1.41 -0.06,0.11 -0.12,0.2 -0.19,0.27 -0.73,-0.48 -1.21,-1.3 -1.21,-2.25 V 30.86 c 0,-1.49 1.19,-2.68 2.68,-2.68 z M 20.11,32.6 c 2.76,0 5.96,2.78 5.96,7.25 v 7.14 c 0,0.78 0.18,1.42 0.46,1.93 -0.18,0.03 -0.36,0.05 -0.54,0.05 H 13.71 c -0.17,0 -0.32,-0.01 -0.48,-0.04 0.04,-0.05 0.07,-0.11 0.1,-0.16 0.25,-0.45 0.39,-1.05 0.39,-1.91 V 39.5 c 0,-1.63 0.64,-3.38 1.76,-4.7 1.11,-1.32 2.69,-2.2 4.63,-2.2 z m 23.02,0 c 2.76,0 5.97,2.78 5.97,7.25 v 7.14 c 0,0.78 0.17,1.42 0.45,1.93 -0.17,0.03 -0.36,0.05 -0.54,0.05 H 36.73 c -0.16,0 -0.32,-0.01 -0.47,-0.04 0.03,-0.05 0.07,-0.11 0.1,-0.16 0.24,-0.45 0.39,-1.05 0.39,-1.91 V 39.5 c 0,-1.63 0.63,-3.38 1.75,-4.7 1.12,-1.32 2.69,-2.2 4.63,-2.2 z m -25.46,4.39 c -0.71,0 -1.28,0.58 -1.28,1.29 0,0.34 0.13,0.66 0.37,0.9 0.24,0.24 0.57,0.38 0.91,0.38 0.71,0 1.28,-0.57 1.28,-1.28 0,-0.71 -0.57,-1.29 -1.28,-1.29 z m 4.4,0 c -0.71,0 -1.29,0.58 -1.29,1.29 0,0.34 0.14,0.66 0.38,0.9 0.24,0.24 0.56,0.38 0.91,0.38 0.7,0 1.28,-0.57 1.28,-1.28 0,-0.71 -0.58,-1.29 -1.28,-1.29 z m 18.62,0 c -0.71,0 -1.28,0.58 -1.28,1.29 0,0.71 0.57,1.28 1.28,1.28 0.34,0 0.67,-0.14 0.91,-0.38 0.24,-0.24 0.38,-0.56 0.38,-0.9 0,-0.71 -0.58,-1.29 -1.29,-1.29 z m 4.4,0 c -0.71,0 -1.28,0.58 -1.28,1.29 0,0.71 0.57,1.28 1.28,1.28 0.71,0 1.28,-0.57 1.28,-1.28 0,-0.71 -0.57,-1.29 -1.28,-1.29 z m -25.23,3.28 c -0.43,0 -0.88,0.1 -1.35,0.31 0,0 -0.01,0.01 -0.01,0.01 -1.34,0.66 -2.24,1.48 -3.06,2.18 l 0.06,0.03 c -0.13,0.08 -0.22,0.21 -0.24,0.36 0.02,0.15 0.11,0.29 0.24,0.37 l -0.05,0.03 c 0.79,0.66 1.81,1.36 2.94,1.9 0,0.01 0.01,0.01 0.01,0.01 0.97,0.43 2.16,0.44 3.07,-0.02 0,0 0,0 0,0 1.19,-0.61 2.04,-1.22 2.87,-1.81 0.12,-0.09 0.2,-0.23 0.21,-0.38 0.02,-0.16 -0.04,-0.31 -0.15,-0.41 -0.73,-0.69 -1.77,-1.64 -3.13,-2.27 0,0 -0.01,0 -0.01,0 -0.42,-0.19 -0.85,-0.3 -1.31,-0.31 v 0 c -0.03,0 -0.06,0 -0.09,0 z m 23.03,0 c -0.43,0 -0.88,0.1 -1.35,0.31 -0.01,0 -0.01,0.01 -0.02,0.01 -1.34,0.66 -2.24,1.48 -3.05,2.18 l 0.05,0.03 c -0.13,0.08 -0.21,0.21 -0.23,0.36 0.02,0.15 0.1,0.29 0.23,0.37 l -0.05,0.03 c 0.8,0.66 1.82,1.36 2.94,1.9 0.01,0.01 0.01,0.01 0.02,0.01 0.96,0.43 2.16,0.44 3.07,-0.02 0,0 0,0 0,0 1.18,-0.61 2.03,-1.22 2.86,-1.81 0.13,-0.09 0.21,-0.23 0.22,-0.38 0.01,-0.16 -0.05,-0.31 -0.16,-0.41 -0.73,-0.69 -1.76,-1.64 -3.13,-2.27 0,0 0,0 0,0 -0.42,-0.19 -0.86,-0.3 -1.32,-0.31 v 0 c -0.03,0 -0.05,0 -0.08,0 z m -23.01,1 v 0 c 0.02,0 0.03,0 0.05,0 v 0 c 0.29,0 0.57,0.09 0.91,0.25 0.95,0.43 1.7,1.05 2.34,1.63 -0.66,0.47 -1.29,0.93 -2.18,1.38 -0.53,0.27 -1.52,0.28 -2.18,0 -0.82,-0.4 -1.52,-0.92 -2.18,-1.42 0.66,-0.56 1.36,-1.12 2.31,-1.59 0.36,-0.17 0.66,-0.25 0.93,-0.25 z m 23.02,0 v 0 c 0.02,0 0.04,0 0.06,0 v 0 c 0.29,0 0.56,0.09 0.9,0.25 0.95,0.43 1.7,1.05 2.35,1.63 -0.66,0.47 -1.3,0.93 -2.18,1.38 -0.53,0.27 -1.52,0.28 -2.18,0 -0.83,-0.4 -1.53,-0.92 -2.18,-1.42 0.66,-0.56 1.35,-1.12 2.3,-1.59 0.37,-0.17 0.66,-0.25 0.93,-0.25 z" fill="#000000" stroke="none" pointer-events="all" id="path4"/>
        </g>
      </g>
    </g>
  </g>
  <script type="text/javascript"><![CDATA[/*
    # =============================================
    # ===             How to Use                ===
    # =============================================

    Variables with the following prefix:

    _pb_ = bool   parameter ( true or false )
    _pn_ = number parameter ( Int, Float, Real etc )
    _ps_ = string parameter ( string can be entered directly )
    _pc_ = color parameter in hex color code ( #00ff00ff )

    Indicate variable type to be used in Fuxa Properties and binding of Tags, Important! they have to be place within:

    //!export-start
    let _pn_setState = 0;
    //!export-end

    There are 2x Functions to handle sending and receiving data between Fuxa and the SVG

    // Recieve Variables From Fuxa
    function putValue(id, value) { } 
    
    // Send Variables To Fuxa
    function postValue(id, value) {
      console.error('Not defined!');
    }

    The ID is the name of the variable _pn_setState for example and value is the value to be sent or received from Fuxa


    This SVG uses an INT/Number Variable to change the State and State of the SVG:

    0 = Off
    1 = Run
    2 = Warning
    3 = Fault
    4 = Invalid
    5 = Spare

    You can also use the bool field and this will change the state between 0 = off and 1 = run 

    You can also set the color for each state in Fuxa using HEX color code #b4b4b4
    The Default Blink Interval for On and Off is 500ms but can also be changed in Fuxa in MS resolution

    <svg>
      SVG Content Here
      <script>
        JS Script Content Here
      </script>
    </svg>

    */

	  // Global Parameters and Variables

    //!export-start
    let _pn_setState = 0;                     // 0=Off, 1=Run, 2=Warn, 3=Fault, 4=Invalid, 5=Spare
    let _pb_boolState = false;                // true = Run, false = Off
    let _pn_setIntervalOn = 500;              // ms fault color is ON
    let _pn_setIntervalOff = 500;             // ms fault color is OFF
    let _pn_strokeWidth = 1;                  // Global stroke width for all states
    let _pc_fill_0 = 'none';                  // Off
    let _pc_fill_1 = 'none';                  // Run
    let _pc_fill_2 = 'none';                  // Warn
    let _pc_fill_3 = 'none';                  // Fault
    let _pc_fill_4 = 'none';                  // Invalid
    let _pc_fill_5 = 'none';                  // Spare
    let _pc_stroke_0 = '#7f7f7f';             // Off
    let _pc_stroke_1 = '#00ff00';             // Run
    let _pc_stroke_2 = '#ff9500';             // Warn
    let _pc_stroke_3 = '#ff0000';             // Fault
    let _pc_stroke_4 = '#3c3c3c';             // Invalid
    let _pc_stroke_5 = '#dcdcdc';             // Spare
    let _pb_preserveAspectRatio = true;       // true = preserve aspect ratio, false = stretch
    let _pn_rotateAngle = 0;                  // degrees, 0 = no rotation
    let _pn_rotateOffsetX = 0;                // offset from center (0 = center)
    let _pn_rotateOffsetY = 0;                // offset from center (0 = center)
    let _pb_flipHorizontal = false;           // true = flip X
    let _pb_flipVertical = false;             // true = flip Y
    let _pn_flipOffsetX = 0;                  // offset from center (0 = center)
    let _pn_flipOffsetY = 0;                  // offset from center (0 = center)
    let _pn_padding = 0;                      // padding around SVG (default 0)
    //!export-end

    let stateNumber = 0;
    let blinkInterval = null;
    let blinkTimer = null;
    let blinkObserver = null;
    let originalViewBox = null;

    // Get dynamic center from SVG viewBox (uses originalViewBox if available)
    function getCenter() {
      const svg = document.getElementById('fuxaWidget');
      const symbol = document.getElementById('fuxaSymbol');
      if (!svg) return { x: 25.5, y: 25.5 };
      if (symbol && typeof symbol.getBBox === 'function') {
        const bb = symbol.getBBox();
        return { x: bb.x + bb.width / 2, y: bb.y + bb.height / 2 };
      }
      const vbString = originalViewBox || svg.getAttribute('viewBox') || '0 0 51 51';
      const vb = vbString.split(/\s+/).map(parseFloat);
      return { x: (vb[0] + vb[2]) / 2, y: (vb[1] + vb[3]) / 2 };
    }

    // Apply transform to symbol group
    function applyTransform() {
      const symbol = document.getElementById('fuxaSymbol');
      if (!symbol) return;
      const center = getCenter();
      const cx = center.x + _pn_rotateOffsetX + _pn_flipOffsetX;
      const cy = center.y + _pn_rotateOffsetY + _pn_flipOffsetY;
      let transform = '';
      if (_pb_flipHorizontal || _pb_flipVertical) {
        const scaleX = _pb_flipHorizontal ? -1 : 1;
        const scaleY = _pb_flipVertical ? -1 : 1;
        transform += `scale(${scaleX}, ${scaleY}) translate(${scaleX === -1 ? -cx * 2 : 0}, ${scaleY === -1 ? -cy * 2 : 0}) `;
      }
      if (_pn_rotateAngle !== 0) {
        transform += `rotate(${_pn_rotateAngle}, ${cx}, ${cy}) `;
      }
      symbol.setAttribute('transform', transform.trim() || '');
    }

    // Update viewBox with padding
    function updatePadding() {
      const svg = document.getElementById('fuxaWidget');
      const symbol = document.getElementById('fuxaSymbol');
      if (!svg) return;
      const pad = Math.max(0, _pn_padding || 0);
      // Prefer persisted original viewBox (stable) — if not present, persist a computed one using getBBox
      const persisted = svg.getAttribute('data-fuxa-orig-viewbox');
      if (persisted) {
        const vb = persisted.split(/\s+/).map(parseFloat);
        const width = vb[2] - vb[0];
        const height = vb[3] - vb[1];
        const padded = Math.max(width, height);
        const size = padded + 2 * pad;
        const offsetX = (padded - width) / 2;
        const offsetY = (padded - height) / 2;
        svg.setAttribute('viewBox', `${vb[0] - pad - offsetX} ${vb[1] - pad - offsetY} ${size} ${size}`);
        return;
      }
      if (symbol && typeof symbol.getBBox === 'function') {
        // Use the actual content bounds to compute the original viewBox and persist it
        const bb = symbol.getBBox();
        const contentSize = Math.max(bb.width, bb.height);
        const size = contentSize + 2 * pad;
        const cx = bb.x + bb.width / 2;
        const cy = bb.y + bb.height / 2;
        const x = cx - size / 2;
        const y = cy - size / 2;
        const vbString = `${x} ${y} ${size} ${size}`;
        svg.setAttribute('data-fuxa-orig-viewbox', vbString);
        originalViewBox = vbString;
        svg.setAttribute('viewBox', vbString);
        return;
      }
      // Fallback to original viewBox if getBBox is not available
      const vbString = originalViewBox || svg.getAttribute('viewBox') || '0 0 51 51';
      const vb = vbString.split(/\s+/).map(parseFloat);
      const width = vb[2] - vb[0];
      const height = vb[3] - vb[1];
      const padded = Math.max(width, height);
      const size = padded + 2 * pad;
      const offsetX = (padded - width) / 2;
      const offsetY = (padded - height) / 2;
      svg.setAttribute('viewBox', `${vb[0] - pad - offsetX} ${vb[1] - pad - offsetY} ${size} ${size}`);
    }

    // Function to update preserveAspectRatio
    function updatePreserveAspectRatio() {
      const svg = document.getElementById('fuxaWidget');
      if (!svg) return;
      if (_pb_preserveAspectRatio) {
        svg.setAttribute('preserveAspectRatio', 'xMidYMid meet');
      } else {
        svg.setAttribute('preserveAspectRatio', 'none');
      }
    }

    // Function to update the color based on the set state
    function logic() {
      switch (stateNumber) {
        case 0: // off
          setBaseColor(_pc_stroke_0, _pc_fill_0);
          clearBlinking();
          break;
        case 1: // run
          setBaseColor(_pc_stroke_1, _pc_fill_1);
          clearBlinking();
          break;
        case 2: // warning
          setBaseColor(_pc_stroke_2, _pc_fill_2);
          clearBlinking();
          break;
        case 3: // fault
          const onRaw = +_pn_setIntervalOn;
          const offRaw = +_pn_setIntervalOff;
          const on = (onRaw > 0) ? Math.max(50, onRaw) : 0;
          const off = (offRaw > 0) ? Math.max(50, offRaw) : 0;

          if (on === 0 || off === 0) {
            setBaseColor(_pc_stroke_3, _pc_fill_3);
            clearBlinking();
          } else {
            startDualBlinking(_pc_stroke_0, _pc_stroke_3, _pc_fill_0, _pc_fill_3, on, off);
          }
          break;
        case 4: // invalid
          setBaseColor(_pc_stroke_4, _pc_fill_4);
          clearBlinking();
          break;
        case 5: // spare
          setBaseColor(_pc_stroke_5, _pc_fill_5);
          clearBlinking();
          break;
        default:
          console.log('Undefined State');
          clearBlinking();
      }
    }

    // Initial setup
    function init() {
      // ensure no residual timers from previous initializations
      clearBlinking();
      const svg = document.getElementById('fuxaWidget');
      if (svg) {
        // Use or persist the original viewBox to prevent cumulative padding growth
        if (!svg.getAttribute('data-fuxa-orig-viewbox')) {
          svg.setAttribute('data-fuxa-orig-viewbox', svg.getAttribute('viewBox') || '0 0 51 51');
        }
        originalViewBox = svg.getAttribute('data-fuxa-orig-viewbox');
      }
      updatePadding();
      updatePreserveAspectRatio();
      applyTransform();
      logic();
    }

    // Function to update values from Fuxa
    function putValue(id, value) {
      let needsTransform = false;

      if (id === '_pn_setState') {
        stateNumber = value;
        logic();
      }
      if (id === '_pb_boolState') {
        _pb_boolState = value;
        if (_pb_boolState === true) {
          stateNumber = 1;
        } else if (_pb_boolState === false) {
          stateNumber = 0;
        }
        logic();
      }
      if (id === '_pn_setIntervalOn') {
        _pn_setIntervalOn = value;
        if (stateNumber === 3) logic();
      }
      if (id === '_pn_setIntervalOff') {
        _pn_setIntervalOff = value;
        if (stateNumber === 3) logic();
      }
      if (id === '_pn_strokeWidth') _pn_strokeWidth = value;
      if (id === '_pc_fill_0') _pc_fill_0 = value;
      if (id === '_pc_fill_1') _pc_fill_1 = value;
      if (id === '_pc_fill_2') _pc_fill_2 = value;
      if (id === '_pc_fill_3') _pc_fill_3 = value;
      if (id === '_pc_fill_4') _pc_fill_4 = value;
      if (id === '_pc_fill_5') _pc_fill_5 = value;
      if (id === '_pc_stroke_0') _pc_stroke_0 = value;
      if (id === '_pc_stroke_1') _pc_stroke_1 = value;
      if (id === '_pc_stroke_2') _pc_stroke_2 = value;
      if (id === '_pc_stroke_3') _pc_stroke_3 = value;
      if (id === '_pc_stroke_4') _pc_stroke_4 = value;
      if (id === '_pc_stroke_5') _pc_stroke_5 = value;

      // ROTATE
      if (id === '_pn_rotateAngle') { _pn_rotateAngle = value; needsTransform = true; }
      if (id === '_pn_rotateOffsetX') { _pn_rotateOffsetX = value; needsTransform = true; }
      if (id === '_pn_rotateOffsetY') { _pn_rotateOffsetY = value; needsTransform = true; }

      // FLIP
      if (id === '_pb_flipHorizontal') {
        _pb_flipHorizontal = (value === true || value === 1 || value === '1');
        needsTransform = true;
      }
      if (id === '_pb_flipVertical') {
        _pb_flipVertical = (value === true || value === 1 || value === '1');
        needsTransform = true;
      }
      if (id === '_pn_flipOffsetX') { _pn_flipOffsetX = value; needsTransform = true; }
      if (id === '_pn_flipOffsetY') { _pn_flipOffsetY = value; needsTransform = true; }

      // PADDING
      if (id === '_pn_padding') {
        _pn_padding = Math.max(0, +value || 0);
        updatePadding();
        needsTransform = true;
      }

      // PRESERVE ASPECT RATIO
      if (id === '_pb_preserveAspectRatio') {
        _pb_preserveAspectRatio = (value === true || value === 1 || value === '1');
        updatePreserveAspectRatio();
      }

      if (needsTransform) {
        applyTransform();
      }
    }

    // Function to send values back to Fuxa (placeholder)
    function postValue(id, value) {
      console.log('Not defined!');
    }

    // Make postValue globally available only if not already set by Fuxa
    if (!window.postValue) {
      window.postValue = postValue;
    }

    // Initialize the SVG
    init();

    // Re-run init when the page becomes visible (e.g., returning from editor)
    document.addEventListener('visibilitychange', function() {
      if (document.visibilityState === 'visible') {
        setTimeout(() => init(), 10);
      }
    });

    // Function to update the color for the base shape group
    function setBaseColor(strokeColor, fillColor) {
      const symbol = document.getElementById('fuxaSymbol');
      if (!symbol) return;
      symbol.querySelectorAll('*').forEach(function(elem) {
        try {
          // Set both presentation attributes and inline styles so existing style attributes
          // (e.g., style="fill:none") are overridden by the inline style setting.
          if (fillColor !== undefined && fillColor !== null) {
            elem.setAttribute('fill', fillColor);
            elem.style.fill = fillColor;
          }
          if (strokeColor !== undefined && strokeColor !== null) {
            elem.setAttribute('stroke', strokeColor);
            elem.style.stroke = strokeColor;
          }
          elem.setAttribute('stroke-width', _pn_strokeWidth);
          elem.style.strokeWidth = String(_pn_strokeWidth);
        } catch (e) {
          // Ignore if an element doesn't accept a property
        }
      });
      applyTransform();
    }

    // DUAL BLINK: onTime (fault), offTime (off) - simple top-level tick-by-id function
    // This follows the original approach — a named top-level tick function that schedules itself.
    function fuxa_blink_tick(svgId) {
      const svg = document.getElementById(svgId);
      if (!svg) {
        clearBlinking();
        return;
      }
      if (!svg || !svg._fuxa_blink_params) return;
      const p = svg._fuxa_blink_params;
      if (svg._fuxa_blink_state) {
        setBaseColor(p.strokeOn, p.fillOn);
        svg._fuxa_blink_state = false;
        svg._fuxa_blink_timer = setTimeout(function() { fuxa_blink_tick(svgId); }, p.onTime);
      } else {
        setBaseColor(p.strokeOff, p.fillOff);
        svg._fuxa_blink_state = true;
        svg._fuxa_blink_timer = setTimeout(function() { fuxa_blink_tick(svgId); }, p.offTime);
      }
    }

    function startDualBlinking(strokeOff, strokeOn, fillOff, fillOn, onTime, offTime) {
      clearBlinking();
      // Ensure values are numbers
      onTime = Number(onTime) || 0;
      offTime = Number(offTime) || 0;
      const id = 'fuxaWidget';
      const svg = document.getElementById(id);
      if (!svg) return;
      svg._fuxa_blink_params = { strokeOff, strokeOn, fillOff, fillOn, onTime, offTime };
      svg._fuxa_blink_state = true;
      // start the tick by id (named function) for reliability
      fuxa_blink_tick(id);

      blinkObserver = new MutationObserver((mutations) => {
        mutations.forEach((mutation) => {
          mutation.removedNodes.forEach((node) => {
            if (node && node.id === 'fuxaWidget') {
              clearBlinking();
              if (blinkObserver) {
                blinkObserver.disconnect();
                blinkObserver = null;
              }
            }
          });
        });
      });
      blinkObserver.observe(document.body, { childList: true, subtree: true });
    }

    // FULL CLEAR BLINKING
    function clearBlinking() {
      if (blinkInterval) {
        clearInterval(blinkInterval);
        blinkInterval = null;
      }
      if (blinkTimer) {
        clearTimeout(blinkTimer);
        blinkTimer = null;
      }
      if (blinkObserver) {
        blinkObserver.disconnect();
        blinkObserver = null;
      }
      const svg = document.getElementById('fuxaWidget');
      if (svg) {
        if (svg._fuxa_blink_timer) { clearTimeout(svg._fuxa_blink_timer); svg._fuxa_blink_timer = null; }
        if (svg._fuxa_blink_params) { svg._fuxa_blink_params = null; }
        if (svg._fuxa_blink_state !== undefined) { svg._fuxa_blink_state = null; }
      }
    }]]></script>
</svg>
